# GUI Package

Main application package for the URC Rover Control interface.

## Usage

Run from the project root:
```bash
python3 -m gui.main
```

## Configuration

See `config.py` for IPs, Ports, and Camera names.
